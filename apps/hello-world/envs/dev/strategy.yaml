apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: hello-world-app
spec:
  strategy:
    blueGreen:
      autoPromotionEnabled: true
      autoPromotionSeconds: 20
      scaleDownDelaySeconds: 300
      activeService: dev-hello-world-active
      previewService: dev-hello-world-preview
      previewReplicaCount: 1
      activeMetadata:
        labels:
          role: active
      previewMetadata:
        labels:
          role: preview
      prePromotionAnalysis:
        templates:
        - templateName: dev-smoke-tests
        args:
        - name: route-host
          value: dev-hello-world-preview-hello-world-dev.apps.e3c5k7g5w3o7p7w.5vc5.p1.openshiftapps.com
