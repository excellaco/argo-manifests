replicaCount: 2

image:
  repository: quay.io/openshiftdemos/rollouts-demo
  tag: "blue"

service:
  port: 8080
  previewPort: 8081
  targetPort: 8080

livenessProbe:
  initialDelaySeconds: 15
  periodSeconds: 20

smokeTest:
  routeHost: placeholder  # overridden per env

revisionHistoryLimit: 2
